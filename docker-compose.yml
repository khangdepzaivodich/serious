services:
  # =========================
  # SQL SERVER
  # =========================
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: sqlserver
    environment:
      SA_PASSWORD: ${SA_PASSWORD}
      ACCEPT_EULA: "Y"
    ports:
      - "1433:1433"
    volumes:
      - sql_data:/var/opt/mssql
    networks:
      - backend

  # =========================
  # MONGODB
  # =========================
  mongodb:
    image: mongo:7
    container_name: mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    networks:
      - backend

  # =========================
  # REDIS
  # =========================
  redis:
    image: redis:7
    container_name: redis
    ports:
      - "6379:6379"
    networks:
      - backend

  # =========================
  # IDENTITY SERVICE
  # =========================
  identity-service:
    build:
      context: ./Services/IdentityService
    restart: always
    ports:
      - "7093:8080"
    depends_on:
      - sqlserver
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ConnectionStrings__DefaultConnection: ${IDENTITY_DB}
      JwtSettings__RsaPrivateKey: ${JWT_RSA_PRIVATE_KEY}
      JwtSettings__RsaPublicKey: ${JWT_RSA_PUBLIC_KEY}
      SmtpSettings__Host: ${SMTP_HOST}
      SmtpSettings__Port: ${SMTP_PORT}
      SmtpSettings__Username: ${SMTP_USERNAME}
      SmtpSettings__Password: ${SMTP_PASSWORD}
      SmtpSettings__From: ${SMTP_FROM}
      SmtpSettings__FromDisplayName: ${SMTP_FROM_NAME}
      CloudinarySettings__CloudName: ${CLOUDINARY_CLOUD_NAME}
      CloudinarySettings__ApiKey: ${CLOUDINARY_API_KEY}
      CloudinarySettings__ApiSecret: ${CLOUDINARY_API_SECRET}
    networks:
      - backend

  # =========================
  # CATALOG SERVICE
  # =========================
  catalog-service:
    build:
      context: ./Services/CatalogService
    restart: always
    ports:
      - "7103:8080"
    depends_on:
      - sqlserver
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ConnectionStrings__DefaultConnection: ${CATALOG_DB}
      CloudinarySettings__CloudName: ${CLOUDINARY_CLOUD_NAME}
      CloudinarySettings__ApiKey: ${CLOUDINARY_API_KEY}
      CloudinarySettings__ApiSecret: ${CLOUDINARY_API_SECRET}
      JwtSettings__RsaPublicKey: ${JWT_RSA_PUBLIC_KEY}
    networks:
      - backend

  # =========================
  # ORDERING SERVICE
  # =========================
  ordering-service:
    build:
      context: ./Services/OrderingService
    restart: always
    ports:
      - "7076:8080"
    depends_on:
      - sqlserver
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ConnectionStrings__DefaultConnection: ${ORDERING_DB}
      JwtSettings__RsaPublicKey: ${JWT_RSA_PUBLIC_KEY}
    networks:
      - backend

  # =========================
  # BASKET SERVICE (REDIS)
  # =========================
  basket-service:
    build:
      context: ./Services/BasketService
    restart: always
    ports:
      - "7021:8080"
    depends_on:
      - redis
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      Redis__ConnectionString: ${REDIS_CONNECTION}
      ApiSettings__CatalogUrl: "http://catalog-service:8080"
      ApiSettings__IdentityUrl: "http://identity-service:8080"
      JwtSettings__RsaPublicKey: ${JWT_RSA_PUBLIC_KEY}
    networks:
      - backend

  # =========================
  # DISCOUNT SERVICE (MONGO)
  # =========================
  discount-service:
    build:
      context: ./Services/DiscountService
    restart: always
    ports:
      - "7002:8080"
    depends_on:
      - mongodb
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      Mongo__ConnectionString: ${MONGO_CONNECTION}
      Mongo__Database: ${MONGO_DB_DISCOUNT}
      JwtSettings__RsaPublicKey: ${JWT_RSA_PUBLIC_KEY}
    networks:
      - backend

  # =========================
  # CHAT SERVICE (MONGO + REDIS)
  # =========================
  chat-service:
    build:
      context: ./Services/ChatService
    restart: always
    ports:
      - "7229:8080"
    depends_on:
      - mongodb
      - redis
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      Mongo__ConnectionString: ${MONGO_CONNECTION}
      Mongo__Database: ${MONGO_DB_CHAT}
      Redis__ConnectionString: ${REDIS_CONNECTION}
      JwtSettings__RsaPublicKey: ${JWT_RSA_PUBLIC_KEY}
    networks:
      - backend

# =========================
# NETWORKS
# =========================
networks:
  backend:

# =========================
# VOLUMES
# =========================
volumes:
  sql_data:
  mongo_data:
